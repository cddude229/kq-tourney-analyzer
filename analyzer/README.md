# analyzer

Takes a Hivemind export (from Abby) of a tourney's data set, re-analyzes it using a state machine for game state, and 
then writes out game/set/match summaries that can be imported into a DB.  (May eventually write to the DB, tbd)

TODO